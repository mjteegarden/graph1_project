[pytest]
addopts = -ra
testpaths = tests
python_files = test_*.py
norecursedirs = .* build dist Lib site-packages .venv venv env
markers =
    perf: performance / load tests (opt-in)
    concurrency: multi-threaded tests (opt-in)